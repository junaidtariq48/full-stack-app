version: "3"
services:
  mongodb:
    container_name: mongodb
    image: mongo
    volumes:
      - ~/mongo:/data/db
    ports:
      - "27017:27017"
    networks:
      - full-stack-network

networks:
  full-stack-network:
    driver: bridge
